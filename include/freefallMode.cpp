#include "freefallMode.h"

// mode = 0
void freefallMode(){ 
    // buttons disabled
    // Start JumpLog
    // read both pressure sensors
    // compare -> ERROR if different
    // smooth sensor readings
    // display altitude in 100m steps -> font: u8g2_font_7Segments_26x42_mn 
    // check freefall speed to change to canopy mode (1)
}